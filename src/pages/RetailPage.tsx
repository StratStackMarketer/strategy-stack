import { VerticalPage } from './VerticalPage';
import { retailConfig } from '../data/verticals';

export function RetailPage() {
  return <VerticalPage config={retailConfig} />;
}

export default RetailPage;
